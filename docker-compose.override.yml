services:
  malla-web:
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:5020/').read()\""]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 20s
